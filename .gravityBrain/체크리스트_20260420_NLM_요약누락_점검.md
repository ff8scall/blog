# ✅ 체크리스트: NLM 요약 누락 및 필드 유실 대응 (v1.0)

## 📅 날짜: 2026-04-20
## 🎯 목표: NLM이 필드를 누락하더라도 본문 데이터를 활용하여 완성도 높은 기사를 발행하도록 시스템 복원력 강화.

---

### 1️⃣ 단계: 파서(Parser) 복원력 강화 (`nlm_parser.py`)
- [x] `_post_process` 함수 내에 `kor_summary` 부재 시 `kor_content` 첫 문단 추출 로직 구현.
- [x] `eng_summary` 부재 시 `eng_content` 또는 제목 활용 로직 강화.
- [x] 필드 추출 실패 시에도 'Untitled' 방지를 위한 제목 추출 로직 정밀화.

### 2️⃣ 단계: 프롬프트 엄격성 강화 (`notebooklm_prep.py`)
- [x] `PROMPT_MODE_B` 내 "STRICT FORMATTING" 섹션 보강.
- [x] "모든 필드(ID, TITLE, SUMMARY, CONTENT 등)를 단 하나도 빠짐없이 출력하라"는 강제 지침 추가.

### 3️⃣ 단계: 테스트 및 검증
- [x] 기존에 문제가 된 `hardware_report.md`를 로컬에서 다시 파싱하여 요약이 생성되는지 확인.
- [x] `create_hugo_post` 호출 시 `summary_list`가 비어있지 않은지 최종 검토.
- [x] **실제 복구 완료**: 오늘 생성된 하드웨어 기사 10건에 대해 요약 보정 후 재배포 완료. (2026-04-20)

---
**[비고]**
- NLM의 할루시네이션이나 출력 생략은 외부 API 특성상 완전 차단이 어려우므로, 시스템 내부의 '자동 복구(Fallback)' 로직이 가장 확실한 해결책임.
