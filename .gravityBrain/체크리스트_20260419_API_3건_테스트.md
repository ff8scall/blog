# ✅ 체크리스트: API 기반 기사 3건 생성 테스트

## 📅 날짜: 2026-04-19
## 🎯 목표: `legacy` 모드(API 직접 호출)를 통해 3건의 고품질 기사를 생성하고 파이프라인 전체 로직을 검증함.

---

### 1️⃣ 단계: 사전 확인
- [x] `automation/news_main.py`의 인자값 확인 (`--mode legacy --limit 3`)
- [x] API Throttling(20초) 설정 확인
- [x] 이미지 매니저(Tiered Strategy) 활성화 여부 확인

### 2️⃣ 단계: 실행 및 모니터링
- [x] `py automation/news_main.py --mode legacy --limit 3` 실행
- [x] 실시간 로그를 통한 기사 요약 및 이미지 생성 과정 모니터링
- [x] 20초 대기 로직 작동 확인

### 3️⃣ 단계: 결과 검증
- [x] `content/` 폴더 내 마크다운 파일 3건 생성 확인
- [x] 한/영 미러링 퀄리티 검토 (Why & So What 포함 여부)
- [x] 이미지 경로 및 SEO용 슬러그(`{cluster}-{slug}`) 형식 확인

### 4️⃣ 단계: 마무리
- [x] `MEMORY.md` 및 `SYSTEM_MAP.md` 현행화
- [x] 최종 보고

---

### 🚀 특이사항
- 무료 티어 API 안정성을 위해 20초 간격 유지가 필수적임.
- 생성된 기사는 Hugo 엔진을 통해 즉시 배포 가능해야 함.
